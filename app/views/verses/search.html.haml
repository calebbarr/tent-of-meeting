%title
	Tent of Meeting - Search
#books{:align => "left", :style => "padding-left:25%"}
	=link_to "<<<", books_index_path
-if @results.length > 0 then
	%table{:align => "center", :width => "50%"}
		- @results.each do |result|
			- book = result[:book]
			- chapter = result[:chapter]
			- verse = result[:verse]
			- content = result[:content]
			%tr
				%td{:valign => "top", :align => "left", :width => "25%"}
					= link_to book.name+" ", "/"+book.name
					= link_to chapter.name, "/"+book.name+"/"+chapter.name.to_s
					\:
					= link_to verse.name, "/"+book.name+"/"+chapter.name.to_s+"/"+verse.name.to_s
				%td
					=content
-else
	%p
		no results for - "
		=@query
		"